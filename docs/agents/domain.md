# Domain docs

Lampwright is a single-context repository.

Before engineering work:

- Read `CONTEXT.md` for canonical domain language.
- Read relevant decisions under `docs/adr/`.
- Use canonical terms in issues, hypotheses, tests, and implementation.
- Surface conflicts with an ADR instead of silently overriding it.
